import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  modal: {
    display: 'flex',
    flexDirection: 'column',
  },
  root: {
    height: '100%',
  },
  container: {
    maxHeight: '100%',
  },
});
