import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  modal: {
    display: 'flex',
    flexDirection: 'column',
  },
  root: {
    width: '100%',
  },
  container: {
    maxHeight: 440,
  },
});
